fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ML / AI models
faster-whisper>=1.0.0        # Whisper STT (CTranslate2 backend — 4x faster than openai-whisper on CPU)
transformers>=4.40.0         # HuggingFace: CLIP, BLIP-2
torch>=2.2.0                 # PyTorch (CPU build — no CUDA needed for MVP)
Pillow>=10.0.0               # Image processing

# NLP
spacy>=3.7.0                 # Product entity extraction

# Audio
ffmpeg-python>=0.2.0         # Python wrapper for ffmpeg (audio format conversion)

# Utilities
python-multipart>=0.0.9      # FastAPI file upload support
python-dotenv>=1.0.0         # .env file loading
httpx>=0.27.0                # Async HTTP client

# Database (MySQL)
sqlalchemy>=2.0.0            # ORM
pymysql>=1.1.0               # MySQL driver (pure Python, no native libs needed)
cryptography>=42.0.0         # Required by pymysql for caching_sha2_password auth

# Auth
passlib[bcrypt]>=1.7.4       # Password hashing
python-jose[cryptography]>=3.3.0  # JWT tokens
